import React from 'react';
import PricingText from '../../components/PricingText';
import './styles.scss';

const Pricing = props => {
    return (
    // Pricing Text component
        < PricingText />
    )
};

export default Pricing;
